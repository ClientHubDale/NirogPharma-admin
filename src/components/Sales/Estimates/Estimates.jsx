import TransactionListPage from '@/components/Transactions/TransactionListPage'

/** Sales › Estimates — the shared sales list configured for estimates (see shared/docTypes.js). */
export default function Estimates() {
  return <TransactionListPage key="ESTIMATE" type="ESTIMATE" />
}
