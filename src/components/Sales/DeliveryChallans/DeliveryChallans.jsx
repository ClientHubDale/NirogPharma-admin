import TransactionListPage from '@/components/Transactions/TransactionListPage'

/** Sales › Delivery Challans — the shared sales list configured for delivery challans (see shared/docTypes.js). */
export default function DeliveryChallans() {
  return <TransactionListPage key="DELIVERY_CHALLAN" type="DELIVERY_CHALLAN" />
}
