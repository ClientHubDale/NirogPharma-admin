import PaymentListPage from '../shared/PaymentListPage'

/** Finance › Payment In — money received from customers (see shared/paymentTypes.js). */
export default function PaymentIn() {
  return <PaymentListPage key="IN" type="IN" />
}
