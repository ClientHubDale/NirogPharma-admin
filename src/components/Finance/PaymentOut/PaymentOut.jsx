import PaymentListPage from '../shared/PaymentListPage'

/** Finance › Payment Out — money paid to suppliers (see shared/paymentTypes.js). */
export default function PaymentOut() {
  return <PaymentListPage key="OUT" type="OUT" />
}
