import PartyListPage from '../shared/PartyListPage'

/** Parties › Suppliers — the shared party page configured for suppliers (see shared/partyTypes.js). */
export default function Suppliers() {
  return <PartyListPage key="SUPPLIER" type="SUPPLIER" />
}
