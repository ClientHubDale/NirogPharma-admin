import PartyListPage from '../shared/PartyListPage'

/** Parties › Customers — the shared party page configured for customers (see shared/partyTypes.js). */
export default function Customers() {
  return <PartyListPage key="CUSTOMER" type="CUSTOMER" />
}
